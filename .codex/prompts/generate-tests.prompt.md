# Generate Tests

```text
Read AGENTS.md, CONTEXT.md, and .codex/agents/tester.md.

Generate or update tests for: [REPLACE: file path or feature name]

Coverage requirements:
- Happy path
- Edge cases
- Error or failure paths

Constraints:
- Reuse existing test patterns in the repo
- Keep the tests narrowly scoped to the behavior being changed

When done:
1. Run the relevant tests.
2. Report any gaps you could not cover cleanly.
```
